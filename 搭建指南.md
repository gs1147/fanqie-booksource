# 番茄小说书源服务完整搭建教程

## 项目概述

为阅读 App（legado）搭建番茄小说书源服务，实现搜索、目录、正文解密的完整链路。

### 技术架构

```
阅读App ──搜索──→ novel.snssdk.com（番茄移动端API，无需签名）
   │
   ├──目录/详情──→ FastAPI 中间层 ──爬取──→ fanqienovel.com（字体加密HTML）
   │                    │
   └──正文──────────→ FastAPI 中间层 ──解密──→ charset.json 字符映射
```

---

## 一、硬件资源要求

### 1.1 最低配置

| 资源 | 最低要求 | 推荐配置 |
|------|----------|----------|
| CPU | 1 核 | 1 核（无计算密集任务） |
| 内存 | 512 MB | 1 GB |
| 磁盘 | 1 GB | 2 GB（含系统+Python 环境） |
| 带宽 | 1 Mbps | 5 Mbps（同时服务多个用户） |

### 1.2 实际资源占用

以 1 核 1.6 GB 内存的云服务器为例（同时运行 xray 代理服务）：

| 指标 | 用量 | 说明 |
|------|------|------|
| 内存 | ~44 MB | 峰值 44.3 MB，含 Python 运行时 |
| CPU | 接近 0 | 仅请求时短暂消耗，负载 0.01 |
| 磁盘 | ~186 MB | 含 Python venv 虚拟环境 |
| 网络 | 5-50 KB/次 | 每次请求传输量极小 |

### 1.3 服务器推荐

**国内云服务器（推荐）：**
- 阿里云 ECS 共享型 s6（1 核 1 GB）：~30 元/月
- 腾讯云轻量应用服务器（1 核 1 GB）：~30 元/月
- 华为云 HECS（1 核 1 GB）：~30 元/月

**为什么推荐国内服务器：**
- 番茄小说服务器在国内，延迟低（< 50ms）
- 无需代理，直连即可
- 国内访问速度快

**国外服务器（不推荐）：**
- 延迟高（200-500ms），正文加载慢
- 可能被番茄封禁 IP
- 如需使用，建议搭配国内 CDN 中转

---

## 二、软件资源

### 2.1 操作系统

| 系统 | 版本 | 说明 |
|------|------|------|
| Ubuntu | 20.04 / 22.04 / 24.04 | 推荐，兼容性最好 |
| Debian | 11 / 12 | 轻量，适合小内存 |
| CentOS | 7 / 8 / Stream | 稳定，但包较旧 |
| Alibaba Cloud Linux | 3 | 阿里云专用，兼容 CentOS |

### 2.2 Python 环境

| 依赖 | 版本 | 用途 |
|------|------|------|
| Python | 3.8+ | 运行环境 |
| pip | 最新版 | 包管理器 |
| venv | 内置 | 虚拟环境 |

### 2.3 Python 依赖包

| 包名 | 版本 | 用途 |
|------|------|------|
| fastapi | 0.100+ | Web 框架，提供 API 和管理界面 |
| uvicorn | 0.23+ | ASGI 服务器，运行 FastAPI |
| httpx | 0.24+ | HTTP 客户端，调用番茄 API |
| lxml | 4.9+ | HTML 解析，提取书籍信息和正文 |

### 2.4 系统工具

| 工具 | 用途 |
|------|------|
| systemd | 服务管理，开机自启 |
| curl / wget | 测试 API |
| git | 可选，用于更新 charset.json |

---

## 三、番茄小说加密机制

### 3.1 字体加密原理

番茄小说网页版使用自定义字体加密正文内容：

- 正文 `<p>` 标签中的文字使用 Unicode 私有区字符（U+E3F0 ~ U+E57B，即 58344 ~ 58715）
- 每个加密字符对应一个真实汉字，映射关系通过 WOFF2 字体文件动态生成
- 字体文件 URL 在页面 HTML 的 `<style>` 中指定（`font-family: "xxx"`，CDN 域名为 `bytetos.com`）

### 3.2 解密方案

使用 `charset.json` 静态映射表解密，无需下载字体文件：

```python
CODE = [[58344, 58715], [58345, 58716]]  # 两组编码范围

def decode_text(text: str) -> str:
    result = []
    for char in text:
        code = ord(char)
        for i, (start, end) in enumerate(CODE):
            if start <= code <= end:
                bias = code - start
                if bias < len(CHARSET[i]):
                    result.append(CHARSET[i][bias])
                else:
                    result.append(char)
                break
        else:
            result.append(char)
    return "".join(result)
```

`charset.json` 包含两组各 372 个字符的映射数组，来源于 [fanqienovel-downloader](https://github.com/ying-ck/fanqienovel-downloader) 项目。

---

## 四、搜索方案

### 4.1 问题：Web 端 API 有反爬

番茄小说 Web 端搜索 API（`fanqienovel.com/api/author/search/search_book/v1`）需要：
- `a_bogus` 签名参数（字节跳动反爬算法）
- 滑块验证码（`bdturing-verify` 头部）

自动破解不可行。

### 4.2 解决：移动端 API

参考 GitHub 上 [aoaostar/legado](https://github.com/aoaostar/legado)（5200+ ★）的番茄小说书源，发现番茄的**移动端 API 不需要签名**：

```
GET https://novel.snssdk.com/api/novel/channel/homepage/search/search/v1/
参数:
  device_platform = android
  parent_enterfrom = novel_channel_search.tab.
  offset = 0
  aid = 1967          # 番茄小说 App ID
  q = {搜索关键词}
```

- 不需要 cookie、不需要签名、不需要登录
- 返回 JSON 格式，包含 `title`、`book_id`、`author`、`category`、`score` 等字段
- 每页返回约 10 个结果

### 4.3 legado 书源搜索规则

```json
{
    "searchUrl": "https://novel.snssdk.com/api/novel/channel/homepage/search/search/v1/?device_platform=android&parent_enterfrom=novel_channel_search.tab.&offset={{(page-1)*10}}&aid=1967&q={{key}}",
    "ruleSearch": {
        "bookList": "$.data.ret_data[*]",
        "name": "title##<em>|</em>|《|》",
        "author": "author",
        "bookUrl": "http://{服务器地址}/info?book_id={{$.book_id}}",
        "coverUrl": "$.thumb_url",
        "intro": "$.abstract"
    }
}
```

---

## 五、完整部署教程

### 5.1 安装系统依赖

```bash
# Ubuntu / Debian
apt update && apt install -y python3 python3-pip python3-venv curl

# CentOS / Alibaba Cloud Linux
yum install -y python3 python3-pip curl
```

### 5.2 创建项目目录

```bash
mkdir -p /opt/fanqie
cd /opt/fanqie
```

### 5.3 创建虚拟环境

```bash
python3 -m venv venv
source venv/bin/activate
```

### 5.4 安装 Python 依赖

```bash
pip install fastapi uvicorn httpx lxml
```

### 5.5 创建 charset.json

从 [fanqienovel-downloader](https://github.com/ying-ck/fanqienovel-downloader) 项目获取 `charset.json`，放置于 `/opt/fanqie/charset.json`。

格式示例：
```json
{
    "charset": [
        ["字", "符", "映", "射", "..."],  // 第一组 372 个字符
        ["字", "符", "映", "射", "..."]   // 第二组 372 个字符
    ]
}
```

### 5.6 创建服务端代码

创建 `/opt/fanqie/fanqie_server.py`：

```python
# -*- coding: utf-8 -*-
"""
番茄小说书源服务
"""

import json
import re
import time
import httpx
from urllib.parse import unquote
from fastapi import FastAPI, Query, Request
from fastapi.responses import JSONResponse, HTMLResponse
from fastapi.middleware.cors import CORSMiddleware
from lxml import etree

app = FastAPI(title="番茄小说书源服务")
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

# ============================================================
# 加载字符映射表
# ============================================================
with open("charset.json", "r", encoding="utf-8") as f:
    CHARSET = json.load(f)["charset"]

CODE = [[58344, 58715], [58345, 58716]]

def decode_text(text: str) -> str:
    """解密番茄小说加密文字"""
    result = []
    for char in text:
        code = ord(char)
        for i, (start, end) in enumerate(CODE):
            if start <= code <= end:
                bias = code - start
                if bias < len(CHARSET[i]):
                    result.append(CHARSET[i][bias])
                else:
                    result.append(char)
                break
        else:
            result.append(char)
    return "".join(result)

# ============================================================
# HTTP 客户端
# ============================================================
HEADERS = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
    "Referer": "https://fanqienovel.com/",
}

def get_client():
    return httpx.Client(headers=HEADERS, timeout=15, follow_redirects=True)

# ============================================================
# 搜索（通过 novel.snssdk.com 移动端 API）
# ============================================================
SNSSDK_HEADERS = {
    "User-Agent": "com.ss.android.article.news/7.0.3 (Linux; U; Android 12; zh_CN; Pixel 6)",
}

def search_books(keyword: str) -> list:
    """搜索番茄小说"""
    try:
        with httpx.Client(headers=SNSSDK_HEADERS, timeout=10) as client:
            r = client.get(
                "https://novel.snssdk.com/api/novel/channel/homepage/search/search/v1/",
                params={
                    "device_platform": "android",
                    "parent_enterfrom": "novel_channel_search.tab.",
                    "offset": "0",
                    "aid": "1967",
                    "q": keyword,
                },
            )
        d = r.json()
        books = d.get("data", {}).get("ret_data", [])
        results = []
        seen = set()
        for b in books:
            bid = b.get("book_id", "")
            if bid and bid not in seen:
                seen.add(bid)
                title = re.sub(r"<[^>]+>", "", b.get("title", ""))
                results.append({
                    "book_id": bid,
                    "book_name": title,
                    "book_author": b.get("author", ""),
                    "book_category": b.get("category", ""),
                    "book_score": b.get("score", ""),
                })
        return results
    except Exception as e:
        print(f"[search] Error: {e}")
        return []

# ============================================================
# 书籍详情 + 章节列表
# ============================================================
def get_book_info(book_id: str) -> dict:
    """获取书籍详情和章节列表"""
    with get_client() as client:
        r = client.get(f"https://fanqienovel.com/page/{book_id}")

    ele = etree.HTML(r.text)
    title = (ele.xpath("//h1/text()") or ["未知"])[0]
    author = (ele.xpath('//span[@class="author-name-text"]/text()') or ["未知"])[0]
    desc_list = ele.xpath('//div[@class="page-abstract-content"]/p/text()')
    desc = desc_list[0] if desc_list else ""
    cover_list = ele.xpath('//img[contains(@class,"book-cover")]/@src')
    if not cover_list:
        cover_list = ele.xpath('//meta[@property="og:image"]/@content')
    cover = cover_list[0] if cover_list else ""
    status_list = ele.xpath('//span[@class="info-label-yellow"]/text()')
    status = status_list[0] if status_list else ""

    chapters = []
    for ch in ele.xpath('//div[@class="chapter"]/div/a'):
        ch_text = ch.text or ""
        ch_href = ch.xpath("@href")
        ch_id = ch_href[0].split("/")[-1] if ch_href else ""
        if ch_id:
            chapters.append({"title": decode_text(ch_text), "id": ch_id})

    return {
        "book_id": book_id,
        "title": decode_text(title),
        "author": decode_text(author),
        "desc": decode_text(desc),
        "cover": cover,
        "status": decode_text(status),
        "chapter_count": len(chapters),
        "chapters": chapters,
    }

# ============================================================
# 章节正文
# ============================================================
def get_chapter_content(chapter_id: str) -> str:
    """获取并解密章节正文"""
    with get_client() as client:
        r = client.get(f"https://fanqienovel.com/reader/{chapter_id}")

    html = r.text
    match = re.search(r"window\.__INITIAL_STATE__\s*=\s*({.*?});", html)
    if not match:
        return "正文获取失败"

    try:
        state = json.loads(match.group(1))
        content_html = state["reader"]["chapterData"]["content"]
    except (json.JSONDecodeError, KeyError):
        return "正文解析失败"

    tree = etree.HTML(content_html)
    paragraphs = tree.xpath("//p/text()")
    return "\n".join(decode_text(p) for p in paragraphs if p.strip())

# ============================================================
# API 端点
# ============================================================
@app.get("/search")
def api_search(keyword: str = Query(..., description="搜索关键词")):
    """搜索番茄小说"""
    try:
        results = search_books(keyword)
        for r in results:
            r["book_url"] = f"https://fanqienovel.com/page/{r['book_id']}"
        return {"code": 0, "data": results}
    except Exception as e:
        return {"code": -1, "msg": str(e)}

@app.get("/info")
def api_info(book_id: str = Query(..., description="书籍ID")):
    """获取书籍详情和章节列表"""
    try:
        info = get_book_info(book_id)
        return {"code": 0, "data": info}
    except Exception as e:
        return {"code": -1, "msg": str(e)}

@app.get("/content")
def api_content(chapter_id: str = Query(..., description="章节ID")):
    """获取解密后的章节正文"""
    try:
        content = get_chapter_content(chapter_id)
        return {"code": 0, "data": {"content": content}}
    except Exception as e:
        return {"code": -1, "msg": str(e)}

@app.get("/booksource")
def api_booksource(request: Request, host: str = Query(None, description="服务地址")):
    """生成 legado 书源 JSON"""
    if not host:
        scheme = request.headers.get("x-forwarded-proto", request.url.scheme)
        req_host = request.headers.get("host", "localhost:8900")
        host = f"{scheme}://{req_host}"
    host = host.rstrip("/")

    source = [
        {
            "bookSourceName": "番茄小说（自建）",
            "bookSourceGroup": "番茄小说",
            "bookSourceUrl": host,
            "enabled": True,
            "enabledExplore": False,
            "lastUpdateTime": int(time.time() * 1000),
            "searchUrl": "https://novel.snssdk.com/api/novel/channel/homepage/search/search/v1/?device_platform=android&parent_enterfrom=novel_channel_search.tab.&offset={{(page-1)*10}}&aid=1967&q={{key}}",
            "ruleSearch": {
                "bookList": "$.data.ret_data[*]",
                "name": "title##<em>|</em>|《|》",
                "author": "author",
                "bookUrl": host + "/info?book_id={{$.book_id}}",
                "coverUrl": "$.thumb_url",
                "intro": "$.abstract",
            },
            "ruleBookInfo": {
                "name": "$.data.title",
                "author": "$.data.author",
                "coverUrl": "$.data.cover",
                "intro": "$.data.desc",
                "lastChapter": "",
                "wordCount": "",
                "tocUrl": host + "/info?book_id={{$.data.book_id}}",
            },
            "ruleToc": {
                "chapterList": "$.data.chapters",
                "chapterName": "$.title",
                "chapterUrl": host + "/content?chapter_id={{$.id}}",
            },
            "ruleContent": {
                "content": "$.data.content",
            },
        }
    ]
    return JSONResponse(content=source)

# ============================================================
# Web 管理界面
# ============================================================
WEB_HTML = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>番茄小说书源</title>
<style>
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;background:#f5f5f5;padding:20px}
.container{max-width:800px;margin:0 auto}
h1{text-align:center;margin-bottom:24px;color:#333}
h1 span{color:#e74c3c}
.card{background:#fff;border-radius:12px;padding:20px;margin-bottom:16px;box-shadow:0 2px 8px rgba(0,0,0,.08)}
h2{font-size:18px;margin-bottom:12px;color:#333}
.btn{display:inline-block;background:#e74c3c;color:#fff;padding:10px 24px;border:none;border-radius:8px;font-size:15px;cursor:pointer;text-decoration:none}
.btn-sm{padding:6px 16px;font-size:13px}
.step{display:flex;align-items:center;gap:12px;margin:8px 0}
.step-num{width:28px;height:28px;background:#e74c3c;color:#fff;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:14px;font-weight:bold}
input[type=text]{width:100%;padding:10px;border:1px solid #ddd;border-radius:8px;font-size:14px}
.result{margin-top:12px;padding:12px;background:#f8f8f8;border-radius:8px;font-size:14px;display:none}
.result.show{display:block}
.api-table{width:100%;border-collapse:collapse}
.api-table td,.api-table th{padding:8px 12px;border:1px solid #eee;text-align:left;font-size:14px}
.api-table th{background:#f8f8f8}
.loading{display:none;color:#888;margin-top:12px}
.loading.show{display:block}
#search-results .book-item{padding:10px;border-bottom:1px solid #eee;cursor:pointer;transition:.2s}
#search-results .book-item:hover{background:#fff5f5}
.book-title{font-weight:bold;font-size:15px}
.book-id{color:#888;font-size:12px}
</style>
</head>
<body>
<div class="container">
<h1><span>🍅</span> 番茄小说书源</h1>

<div class="card">
<h2>📖 导入到阅读App</h2>
<div class="step"><div class="step-num">1</div><div>复制下方书源链接</div></div>
<div class="step"><div class="step-num">2</div><div>打开阅读App → 书源管理 → 右上角+ → 网络导入</div></div>
<div class="step"><div class="step-num">3</div><div>粘贴链接，确认导入</div></div>
<div style="margin-top:16px">
<input type="text" id="source-url" readonly>
<button class="btn btn-sm" onclick="copySource()" style="margin-left:8px">复制链接</button>
</div>
</div>

<div class="card">
<h2>🔍 搜索书籍</h2>
<div style="display:flex;gap:8px;align-items:center">
<input type="text" id="search-input" placeholder="输入书名，如：斗破苍穹" onkeydown="if(event.key==='Enter')doSearch()">
<button class="btn" onclick="doSearch()">搜索</button>
</div>
<div class="loading" id="search-loading">搜索中...</div>
<div id="search-results"></div>
</div>

<div class="card">
<h2>🔗 通过链接添加</h2>
<p style="color:#666;font-size:14px;margin-bottom:12px">粘贴番茄小说链接，自动提取book_id</p>
<input type="text" id="url-input" placeholder="https://fanqienovel.com/page/7143038691944959011">
<button class="btn" onclick="addByUrl()">添加</button>
<div class="result" id="url-result"></div>
</div>

<div class="card">
<h2>📡 API 接口</h2>
<table class="api-table">
<tr><th>接口</th><th>说明</th></tr>
<tr><td><a href="/search?keyword=斗破苍穹">/search?keyword=xxx</a></td><td>搜索书籍</td></tr>
<tr><td><a href="/info?book_id=7143038691944959011">/info?book_id=xxx</a></td><td>书籍详情+目录</td></tr>
<tr><td><a href="/content?chapter_id=7173216089122439711">/content?chapter_id=xxx</a></td><td>章节正文</td></tr>
<tr><td><a href="/booksource">/booksource</a></td><td>legado书源JSON</td></tr>
</table>
</div>
</div>

<script>
var host = location.origin;
document.getElementById("source-url").value = host + "/booksource";

function copySource() {
    var inp = document.getElementById("source-url");
    inp.select();
    document.execCommand("copy");
    alert("已复制！");
}

function doSearch() {
    var kw = document.getElementById("search-input").value.trim();
    if (!kw) return;
    document.getElementById("search-loading").classList.add("show");
    document.getElementById("search-results").innerHTML = "";
    fetch("/search?keyword=" + encodeURIComponent(kw))
    .then(r => r.json())
    .then(data => {
        document.getElementById("search-loading").classList.remove("show");
        var html = "";
        if (data.code === 0 && data.data && data.data.length > 0) {
            data.data.forEach(function(b) {
                html += '<div class="book-item" onclick="showBook(\\'' + b.book_id + '\\')">' +
                    '<div class="book-title">' + (b.book_name || b.book_id) + '</div>' +
                    '<div class="book-id">ID: ' + b.book_id + '</div></div>';
            });
        } else {
            html = '<p style="color:#888">未找到结果</p>';
        }
        document.getElementById("search-results").innerHTML = html;
    })
    .catch(function(e) {
        document.getElementById("search-loading").classList.remove("show");
        document.getElementById("search-results").innerHTML = '<p style="color:#e74c3c">搜索失败：' + e.message + '</p>';
    });
}

function showBook(bookId) {
    var res = document.getElementById("url-result");
    res.classList.add("show");
    res.innerHTML = "正在获取书籍信息...";
    fetch("/info?book_id=" + bookId)
    .then(r => r.json())
    .then(data => {
        if (data.code === 0) {
            var d = data.data;
            res.innerHTML = '<b>《' + d.title + '》</b><br>' +
                '作者：' + d.author + '<br>' +
                '状态：' + d.status + '<br>' +
                '章节数：' + d.chapter_count + '<br><br>' +
                '在阅读App中搜索 "<b>' + d.title + '</b>" 即可找到此书';
        } else {
            res.innerHTML = "获取失败：" + (data.msg || "未知错误");
        }
    })
    .catch(function(e) { res.innerHTML = "请求失败：" + e.message; });
}

function addByUrl() {
    var url = document.getElementById("url-input").value.trim();
    var m = url.match(/page\\/(\\d+)/);
    if (!m) { alert("无法从链接中提取book_id"); return; }
    var bookId = m[1];
    var res = document.getElementById("url-result");
    res.classList.add("show");
    res.innerHTML = "正在获取书籍信息...";
    fetch("/info?book_id=" + bookId)
    .then(r => r.json())
    .then(data => {
        if (data.code === 0) {
            var d = data.data;
            res.innerHTML = '<b>《' + d.title + '》</b><br>' +
                '作者：' + d.author + '<br>' +
                '状态：' + d.status + '<br>' +
                '章节数：' + d.chapter_count + '<br><br>' +
                '在阅读App中搜索 "<b>' + d.title + '</b>" 即可找到此书';
        } else {
            res.innerHTML = "获取失败：" + (data.msg || "未知错误");
        }
    })
    .catch(function(e) { res.innerHTML = "请求失败：" + e.message; });
}
</script>
</body></html>"""

@app.get("/")
def index():
    return HTMLResponse(WEB_HTML)

# ============================================================
# 启动
# ============================================================
if __name__ == "__main__":
    import uvicorn
    print("=" * 50)
    print("🍅 番茄小说书源服务")
    print("=" * 50)
    print(f"访问 http://localhost:8900 获取书源")
    print(f"书源链接: http://localhost:8900/booksource")
    print("=" * 50)
    uvicorn.run(app, host="0.0.0.0", port=8900)
```

### 5.7 测试启动

```bash
cd /opt/fanqie
source venv/bin/activate
python fanqie_server.py
```

访问 `http://服务器IP:8900` 查看管理界面。

### 5.8 配置 systemd 服务

```bash
cat > /etc/systemd/system/fanqie.service << 'EOF'
[Unit]
Description=Fanqie Novel BookSource Service
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/fanqie
ExecStart=/opt/fanqie/venv/bin/python /opt/fanqie/fanqie_server.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

# 重载配置
systemctl daemon-reload

# 启动服务
systemctl start fanqie

# 设置开机自启
systemctl enable fanqie

# 查看状态
systemctl status fanqie
```

### 5.9 防火墙配置

```bash
# Ubuntu (ufw)
ufw allow 8900/tcp

# CentOS (firewalld)
firewall-cmd --permanent --add-port=8900/tcp
firewall-cmd --reload

# 云服务器还需在安全组中放行 8900 端口
```

---

## 六、阅读 App 导入

### 6.1 导入步骤

1. 下载安装 [阅读 App](https://github.com/gedoor/legado/releases)
2. 打开阅读 App → 书源管理
3. 右上角 `+` → 网络导入
4. 输入书源地址：`http://{服务器IP}:8900/booksource`
5. 确认导入

### 6.2 使用方式

- **搜索**：在阅读 App 中搜索书名，自动调用番茄搜索 API
- **阅读**：选择搜索结果后，自动通过服务器获取目录和正文
- **链接添加**：也可以在 Web 管理界面中粘贴番茄小说链接

---

## 七、日常运维

### 7.1 常用命令

```bash
# 查看服务状态
systemctl status fanqie

# 重启服务
systemctl restart fanqie

# 查看日志
journalctl -u fanqie -f

# 查看资源占用
ps aux | grep fanqie_server
```

### 7.2 更新 charset.json

番茄更新字体后，需要更新映射表：

```bash
# 下载最新的 charset.json
# 来源：https://github.com/ying-ck/fanqienovel-downloader
cp charset.json /opt/fanqie/charset.json

# 重启服务使生效
systemctl restart fanqie
```

### 7.3 备份

```bash
# 备份项目目录
tar czf fanqie-backup-$(date +%Y%m%d).tar.gz /opt/fanqie/
```

---

## 八、已知限制

| 问题 | 说明 | 应对方案 |
|------|------|----------|
| 字体映射失效 | 番茄更新字体后 charset.json 需同步更新 | 关注 fanqienovel-downloader 项目更新 |
| 搜索结果偏差 | 移动端 API 返回头条搜索结果，含同人/衍生 | 用户自行筛选 |
| 反爬风险 | 番茄可能对移动端 API 加强反爬 | 暂无预防方案，关注书源社区 |
| IP 封禁 | 服务器 IP 被番茄封禁后无法获取正文 | 更换 IP 或使用代理 |

---

## 九、相关项目

| 项目 | Stars | 用途 |
|------|-------|------|
| [fanqienovel-downloader](https://github.com/ying-ck/fanqienovel-downloader) | 5600+ | charset.json 来源、下载器参考 |
| [aoaostar/legado](https://github.com/aoaostar/legado) | 5200+ | 番茄书源参考（移动端 API 方案） |
| [XIU2/Yuedu](https://github.com/XIU2/Yuedu) | 11500+ | 精品书源合集 |
| [legado 官方](https://github.com/gedoor/legado) | 15000+ | 阅读 App |

---

## 十、完整 API 文档

| 端点 | 方法 | 参数 | 说明 |
|------|------|------|------|
| `/` | GET | — | Web 管理界面 |
| `/search` | GET | `keyword` | 搜索书籍 |
| `/info` | GET | `book_id` | 书籍详情 + 章节列表 |
| `/content` | GET | `chapter_id` | 解密后的章节正文 |
| `/booksource` | GET | `host`（可选） | legado 书源 JSON |

### 响应示例

```json
// GET /search?keyword=斗破苍穹
{
    "code": 0,
    "data": [
        {
            "book_id": "7143038691944959011",
            "book_name": "斗破苍穹",
            "book_author": "天蚕土豆",
            "book_category": "玄幻",
            "book_score": "9.2",
            "book_url": "https://fanqienovel.com/page/7143038691944959011"
        }
    ]
}

// GET /info?book_id=7143038691944959011
{
    "code": 0,
    "data": {
        "book_id": "7143038691944959011",
        "title": "斗破苍穹",
        "author": "天蚕土豆",
        "desc": "...",
        "cover": "https://...",
        "status": "完结",
        "chapter_count": 1496,
        "chapters": [
            {"title": "第一章 陨落的天才", "id": "7173216089122439711"}
        ]
    }
}

// GET /content?chapter_id=7173216089122439711
{
    "code": 0,
    "data": {
        "content": "解密后的正文文本..."
    }
}
```
